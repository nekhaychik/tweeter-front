export enum LoginControls {
  email = 'email',
  password = 'password',
}

export enum SignUpControls {
  email = 'email',
  password = 'password',
  passwordConfirm = 'passwordConfirm',
  username = 'username',
}

export enum SignUpVerifyControls {
  emailCode = 'emailCode',
}

export enum CreateTweetControls {
  text = 'text',
  isComment = 'isComment',
}

export enum SearchControls {
  search = 'search',
}
